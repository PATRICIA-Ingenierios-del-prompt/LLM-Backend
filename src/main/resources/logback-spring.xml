<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender: Only WARN and ERROR so we don't spam the UI -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender: Log all INFO, WARN, ERROR to a file for debugging -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/application.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

    <!-- Silence overly verbose frameworks in file logs if necessary -->
    <logger name="org.springframework" level="INFO" />
    <logger name="dev.langchain4j" level="DEBUG" />
</configuration>
